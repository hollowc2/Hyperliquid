apiVersion: 1

providers:
  - name: hl-engine
    orgId: 1
    folder: "HL Engine"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
