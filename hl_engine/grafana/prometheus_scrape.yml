# Add this job to your existing prometheus.yml scrape_configs:
#
# scrape_configs:
#   - job_name: 'hl_engine'
#     static_configs:
#       - targets: ['<orchestrator-host>:8000']
#     metrics_path: /metrics
#     scrape_interval: 10s

- job_name: 'hl_engine'
  static_configs:
    - targets: ['localhost:8000']
  metrics_path: /metrics
  scrape_interval: 10s
